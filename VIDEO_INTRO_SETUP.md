# 🎬 Настройка видео-интро

## 📋 Что нужно сделать

Для добавления видео-интро при запуске приложения выполните следующие шаги:

### Шаг 1: Скопировать видео и звук файлы

1. Найдите файл `valve.mp4` на вашем рабочем столе
2. Скопируйте его в папку:
   ```
   kizvpn_admin_panel/app/src/main/res/raw/valve.mp4
   ```

3. Найдите файл `valve-intro-sound.mp3` на вашем рабочем столе
4. Скопируйте его в папку:
   ```
   kizvpn_admin_panel/app/src/main/res/raw/valve_intro_sound.mp3
   ```

   **ВАЖНО:** 
   - Видео файл должен называться именно `valve.mp4` (без пробелов и специальных символов)
   - Звук файл должен называться `valve_intro_sound.mp3` (с подчеркиваниями вместо дефисов, так как Android не поддерживает дефисы в именах ресурсов)

### Шаг 2: Проверка

После копирования файлов структура папки `raw` должна быть:
```
kizvpn_admin_panel/app/src/main/res/raw/
├── valve.mp4
└── valve_intro_sound.mp3
```

### Шаг 3: Сборка приложения

1. В Android Studio выполните **Build > Clean Project**
2. Затем **Build > Rebuild Project**
3. Убедитесь, что файл `valve.mp4` отображается в папке `res/raw/` в Project view

## ⚙️ Как это работает

- При запуске приложения сначала показывается `SplashScreen` с видео `valve.mp4` и звуком `valve_intro_sound.mp3`
- Видео и звук запускаются одновременно
- После окончания видео (или через 13 секунд максимум) приложение переходит к основному экрану
- Видео воспроизводится автоматически, без элементов управления
- Звук воспроизводится синхронно с видео
- Можно пропустить интро, тапнув на экран
- Если видео или звук не найдены, приложение автоматически перейдет к основному экрану через 13 секунд

## 🔧 Настройки

Видео настраивается в файле:
- `app/src/main/java/com/kizvpn/admin/ui/screens/SplashScreen.kt`

Если хотите изменить поведение (например, время показа, повтор видео и т.д.), редактируйте этот файл.

## ⚠️ Важно

- Видео должно быть в формате MP4
- Рекомендуется использовать видео небольшого размера для быстрой загрузки
- После добавления файла обязательно выполните **Rebuild Project** в Android Studio

