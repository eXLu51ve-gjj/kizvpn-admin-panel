# 🚀 Руководство по публикации в GitHub

## 📋 Шаг 1: Приватный репозиторий

### 1.1 Инициализация Git (если еще не сделано)

```bash
cd kizvpn_admin_panel
git init
```

### 1.2 Добавление файлов

```bash
# Добавьте все файлы (кроме тех, что в .gitignore)
git add .

# Создайте первый коммит
git commit -m "Initial commit: KIZ VPN Admin Panel v1.0.0"
```

### 1.3 Создание приватного репозитория на GitHub

1. Зайдите на [GitHub](https://github.com)
2. Нажмите "New repository"
3. Имя: `kizvpn-admin-panel` (или другое)
4. Выберите **Private**
5. Не добавляйте README, .gitignore или лицензию (у нас уже есть)
6. Нажмите "Create repository"

### 1.4 Подключение к GitHub

```bash
# Добавьте remote (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/kizvpn-admin-panel.git

# Или используйте SSH:
# git remote add origin git@github.com:YOUR_USERNAME/kizvpn-admin-panel.git

# Отправьте код
git branch -M main
git push -u origin main
```

### 1.5 Добавление дополнительных файлов

Добавьте в приватный репозиторий:

1. **Скриншоты** (если хотите):
   ```bash
   mkdir screenshots
   # Скопируйте скриншоты с рабочего стола в эту папку
   git add screenshots/
   git commit -m "Add screenshots"
   git push
   ```

2. **APK файл** (опционально):
   ```bash
   mkdir releases
   # Скопируйте APK файл в releases/
   git add releases/
   git commit -m "Add APK release"
   git push
   ```

3. **Обновите README**:
   ```bash
   # Переименуйте README_PRIVATE.md в README.md для приватного репозитория
   mv README_PRIVATE.md README.md
   git add README.md
   git commit -m "Update README for private repo"
   git push
   ```

## 📋 Шаг 2: Публичный репозиторий

### 2.1 Подготовка кода

⚠️ **ВАЖНО**: Сначала подготовьте код согласно `SETUP_INSTRUCTIONS.md`

1. Откройте `SETUP_INSTRUCTIONS.md`
2. Следуйте инструкциям для замены всех секретных данных
3. Проверьте все файлы согласно `PUBLIC_RELEASE_CHECKLIST.md`

### 2.2 Создание чистой ветки для публикации

```bash
# Создайте новую ветку для публичной версии
git checkout -b public-release

# Примените все изменения (замена секретных данных)
# ... (следуйте SETUP_INSTRUCTIONS.md)

# Зафиксируйте изменения
git add .
git commit -m "Prepare for public release: remove sensitive data"
```

### 2.3 Создание публичного репозитория на GitHub

1. Создайте **новый** репозиторий на GitHub
2. Имя: `kizvpn-admin-panel-public` (или `kizvpn-admin-panel`)
3. Выберите **Public**
4. Не добавляйте файлы

### 2.4 Публикация

```bash
# Добавьте новый remote для публичного репозитория
git remote add public https://github.com/YOUR_USERNAME/kizvpn-admin-panel-public.git

# Отправьте ветку
git push public public-release:main
```

### 2.5 Настройка публичного репозитория

1. Переименуйте `README_PUBLIC.md` в `README.md`:
   ```bash
   mv README_PUBLIC.md README.md
   git add README.md
   git commit -m "Set public README"
   git push public main
   ```

2. Добавьте скриншоты (если есть)

3. Настройте описание репозитория на GitHub

## 📋 Шаг 3: Проверка

### Проверка приватного репозитория:
- ✅ Все файлы загружены
- ✅ APK файл доступен (если добавлен)
- ✅ README содержит актуальную информацию
- ✅ Скриншоты добавлены (если есть)

### Проверка публичного репозитория:
- ✅ Нет конфиденциальных данных
- ✅ README содержит инструкции по настройке
- ✅ Проект компилируется
- ✅ Все ссылки на секреты заменены

## 🔍 Финальная проверка публичной версии

```bash
# В папке проекта выполните:
grep -r "host.kizvpn.ru" --include="*.kt" --include="*.java"
grep -r "10.10.10" --include="*.kt" --include="*.java"
grep -r "kizvpn" --include="*.kt" --include="*.java" -i

# Если ничего не найдено - отлично! ✅
```

## 📝 Полезные команды Git

```bash
# Посмотреть статус
git status

# Посмотреть историю
git log --oneline

# Отменить последний коммит (не push)
git reset --soft HEAD~1

# Отправить изменения
git push

# Обновить из remote
git pull
```

## 🆘 Если что-то пошло не так

### Удалить секреты из истории Git

```bash
# Используйте git filter-branch или BFG Repo-Cleaner
# НО: лучше создать новый репозиторий с чистой историей
```

### Вернуться к определенному коммиту

```bash
git log --oneline  # найти нужный коммит
git checkout <commit-hash>
```

## ✅ Готово!

Теперь у вас есть:
- ✅ Приватный репозиторий со всеми данными
- ✅ Публичный репозиторий без секретов
- ✅ Инструкции для пользователей
- ✅ Готовность к дальнейшей разработке

---

**Следующий шаг**: Подготовка к публикации в Google Play Market (см. напоминание ниже)

