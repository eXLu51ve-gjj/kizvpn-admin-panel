# 🎨 Замена логотипа приложения

## 📋 Что нужно сделать

Для замены логотипа приложения на `logo7.png` выполните следующие шаги:

### Шаг 1: Скопировать файл логотипа

1. Найдите файл `logo7.png` на вашем рабочем столе
2. Скопируйте его в папку:
   ```
   kizvpn_admin_panel/app/src/main/res/drawable/logo7.png
   ```

### Шаг 2: Создать разные размеры (опционально, но рекомендуется)

Для лучшего качества на разных устройствах создайте иконки разных размеров:

**Размеры для адаптивных иконок:**
- `drawable/logo7.png` - основной файл (минимум 108x108 dp, рекомендуется 512x512 px)

**Размеры для старых версий Android (mipmap):**
Создайте PNG файлы разных размеров в соответствующих папках:

- `mipmap-mdpi/ic_launcher.png` - 48x48 px
- `mipmap-hdpi/ic_launcher.png` - 72x72 px
- `mipmap-xhdpi/ic_launcher.png` - 96x96 px
- `mipmap-xxhdpi/ic_launcher.png` - 144x144 px
- `mipmap-xxxhdpi/ic_launcher.png` - 192x192 px

И для круглых иконок:
- `mipmap-mdpi/ic_launcher_round.png` - 48x48 px
- `mipmap-hdpi/ic_launcher_round.png` - 72x72 px
- `mipmap-xhdpi/ic_launcher_round.png` - 96x96 px
- `mipmap-xxhdpi/ic_launcher_round.png` - 144x144 px
- `mipmap-xxxhdpi/ic_launcher_round.png` - 192x192 px

**Или используйте онлайн инструмент:**
- [Android Asset Studio](https://romannurik.github.io/AndroidAssetStudio/icons-launcher.html) - автоматически создаст все размеры
- Загрузите `logo7.png` и скачайте готовые иконки

### Шаг 3: Обновить foreground drawable

Файл `ic_launcher_foreground.xml` уже настроен на использование логотипа. 
Если вы хотите использовать PNG напрямую, можно изменить `ic_launcher.xml`:

**Вариант А: Использовать PNG напрямую (проще)**

Измените `res/mipmap-anydpi-v26/ic_launcher.xml`:
```xml
<?xml version="1.0" encoding="utf-8"?>
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <background android:drawable="@color/ic_launcher_background"/>
    <foreground android:drawable="@drawable/logo7"/>
</adaptive-icon>
```

**Вариант Б: Оставить как есть (используя XML drawable)**

Если `logo7.png` уже скопирован в `drawable`, обновите `ic_launcher_foreground.xml` чтобы использовать его.

### Шаг 4: Проверка

После копирования файла структура должна быть:
```
kizvpn_admin_panel/app/src/main/res/
├── drawable/
│   └── logo7.png
└── mipmap-anydpi-v26/
    └── ic_launcher.xml (обновлен)
```

### Шаг 5: Сборка приложения

1. В Android Studio выполните **Build > Clean Project**
2. Затем **Build > Rebuild Project**
3. Убедитесь, что файл `logo7.png` отображается в папке `res/drawable/` в Project view

## ⚙️ Текущая конфигурация

Приложение использует адаптивные иконки (Android 8.0+):
- Основная иконка: `@mipmap/ic_launcher`
- Круглая иконка: `@mipmap/ic_launcher_round`

Обе ссылаются на:
- `res/mipmap-anydpi-v26/ic_launcher.xml` - адаптивная иконка
- Foreground: `@drawable/ic_launcher_foreground`
- Background: `@color/ic_launcher_background`

## 🔧 Рекомендации

1. **Размер логотипа:** Рекомендуется квадратное изображение минимум 512x512 px
2. **Прозрачность:** PNG должен поддерживать прозрачность
3. **Центрирование:** Убедитесь, что логотип центрирован, так как система может обрезать края для адаптивных иконок (около 20% по краям)
4. **Фон:** Если у логотипа нет прозрачного фона, он будет отображаться на цветном фоне (настраивается в `colors.xml`)

## ⚠️ Важно

- После добавления файла обязательно выполните **Rebuild Project** в Android Studio
- Если иконка не обновляется на устройстве, удалите приложение и установите заново
- Для тестирования на эмуляторе может потребоваться очистка кеша

