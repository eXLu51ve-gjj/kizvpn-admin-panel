# 🔐 Настройка биометрической аутентификации

## ✅ Что реализовано

Биометрическая аутентификация успешно интегрирована в приложение!

### Возможности:

1. **Автоматическая проверка доступности**
   - Приложение автоматически определяет, поддерживает ли устройство биометрию
   - Поддерживаются: отпечаток пальца, Face ID, распознавание лица

2. **Настройка в Settings**
   - Переключатель для включения/выключения биометрии
   - Отображается статус доступности биометрии на устройстве

3. **Быстрый вход**
   - При включенной биометрии и сохраненных данных автоматически показывается запрос биометрии
   - Кнопка "Войти с биометрией" на экране входа
   - Альтернативный вход через обычный логин

4. **Безопасность**
   - Биометрия используется только для доступа к сохраненным учетным данным
   - Токен и API URL хранятся безопасно в DataStore

## 🎯 Как использовать

### Первый запуск:

1. Войдите обычным способом (введите API URL и токен)
2. Перейдите в **Настройки**
3. Включите переключатель **"Биометрическая аутентификация"**

### Последующие входы:

1. Откройте приложение
2. Если биометрия включена, автоматически появится запрос на биометрию
3. Приложите палец или используйте Face ID
4. Вы автоматически войдете в приложение

Или:
1. Нажмите кнопку **"Войти с биометрией"** на экране входа
2. Приложите палец или используйте Face ID

## ⚙️ Технические детали

### Использованные компоненты:

- **androidx.biometric:biometric:1.1.0** - библиотека для биометрии
- **AuthRepository** - хранение настройки биометрии
- **BiometricHelper** - утилита для работы с биометрией

### Файлы:

- `app/src/main/java/com/kizvpn/admin/util/BiometricHelper.kt` - утилита биометрии
- `app/src/main/java/com/kizvpn/admin/ui/screens/LoginScreen.kt` - экран входа с биометрией
- `app/src/main/java/com/kizvpn/admin/ui/screens/SettingsScreen.kt` - настройки биометрии
- `app/src/main/java/com/kizvpn/admin/data/repository/AuthRepository.kt` - хранилище настроек

## 🔒 Безопасность

- Биометрические данные НЕ хранятся в приложении
- Android обрабатывает биометрию на уровне системы
- Приложение использует биометрию только для подтверждения доступа
- Учетные данные (токен, API URL) хранятся в зашифрованном DataStore

## ⚠️ Важно

1. **Настройка биометрии на устройстве:**
   - Убедитесь, что биометрия настроена на вашем устройстве
   - Настройки → Безопасность → Отпечаток пальца / Face ID

2. **Резервный вход:**
   - Если биометрия не работает, всегда можно войти обычным способом
   - Просто введите API URL и токен вручную

3. **Отключение биометрии:**
   - Перейдите в Настройки
   - Выключите переключатель "Биометрическая аутентификация"
   - После этого вход будет только через обычный логин

## 🐛 Устранение проблем

**Биометрия не работает:**
- Проверьте, настроена ли биометрия на устройстве
- Убедитесь, что настройка "Биометрическая аутентификация" включена в приложении
- Попробуйте перезапустить приложение

**Ошибка "Недоступно на этом устройстве":**
- Ваше устройство не поддерживает биометрию
- Используйте обычный вход через логин/пароль

**Биометрия запрашивается каждый раз:**
- Это нормальное поведение для безопасности
- Каждый вход требует подтверждения биометрии

## ✨ Готово к использованию!

Биометрическая аутентификация полностью настроена и готова к использованию. Просто включите её в настройках и наслаждайтесь быстрым и безопасным входом! 🎉

