# KIZ VPN Admin Panel

Мобильная админ-панель для управления VPN инфраструктурой KIZ VPN.

## Возможности

### ✅ Реализовано

- 🔐 **Авторизация** - вход по JWT токену
- 🏠 **Главный экран** - дашборд с основными разделами
- 💳 **Платежи** - генерация QR-кодов для оплаты через Сбербанк
- 📊 **Базовая навигация** - переходы между экранами

### 🚧 В разработке

- 🌐 **Управление серверами VPN** - просмотр и управление нодами PasarGuard
- 👥 **Управление пользователями** - CRUD операции с пользователями VPN
- 📈 **Статистика** - графики и аналитика
- 🔔 **Уведомления** - push-уведомления о событиях
- 💰 **Биллинг** - полная система управления платежами
- 📱 **Офлайн режим** - кеширование данных

## Технологии

- **Kotlin** - основной язык
- **Jetpack Compose** - UI фреймворк
- **Material Design 3** - дизайн система
- **Retrofit** - HTTP клиент
- **OkHttp** - сетевой слой
- **ZXing** - генерация QR-кодов
- **DataStore** - хранение настроек
- **Coroutines & Flow** - асинхронность

## Структура проекта

```
kizvpn_admin_panel/
├── app/
│   ├── src/main/
│   │   ├── java/com/kizvpn/admin/
│   │   │   ├── data/
│   │   │   │   ├── api/          # API клиенты
│   │   │   │   ├── model/        # Модели данных
│   │   │   │   └── repository/   # Репозитории
│   │   │   ├── ui/
│   │   │   │   ├── components/   # UI компоненты
│   │   │   │   ├── navigation/   # Навигация
│   │   │   │   ├── screens/      # Экраны
│   │   │   │   └── theme/        # Тема
│   │   │   └── util/             # Утилиты
│   │   └── res/                  # Ресурсы
```

## Настройка

### 1. Получение JWT токена

1. Войдите в веб-панель: `https://host.kizvpn.ru/panel/`
2. Откройте DevTools (F12) → вкладка Network
3. Выполните любой запрос в панели
4. Найдите заголовок `Authorization: Bearer <token>` в запросах
5. Скопируйте токен

### 2. Настройка API URL

По умолчанию используется: `https://host.kizvpn.ru/api`

Можно изменить в настройках приложения или на экране входа.

## Генерация QR-кодов для оплаты

Приложение поддерживает генерацию QR-кодов для оплаты через Сбербанк.

**Формат QR-кода:**
- Deep link: `sberbank://transfer?card=НОМЕР&amount=СУММА_В_КОПЕЙКАХ`
- Web fallback: `https://www.sberbank.com/sms/pbpn?requisiteNumber=НОМЕР&sum=СУММА`

**Номер карты:** `2202206815876342`

## API интеграция

### PasarGuard Panel API

Приложение использует PasarGuard Panel API для управления VPN инфраструктурой:

- **Base URL**: `https://host.kizvpn.ru/api`
- **Аутентификация**: JWT токен в заголовке `Authorization: Bearer <token>`

**Основные эндпоинты:**
- `GET /api/system` - информация о системе
- `GET /api/users` - список пользователей
- `POST /api/users` - создать пользователя
- `GET /api/nodes` - список узлов
- `GET /api/stats` - статистика

Подробнее: [PASARGUARD_API_DOCUMENTATION.md](../PASARGUARD_API_DOCUMENTATION.md)

## Сборка

```bash
# Установить зависимости
./gradlew build

# Собрать APK
./gradlew assembleDebug

# Установить на устройство
./gradlew installDebug
```

## Требования

- Android 10 (API 29) и выше
- Интернет соединение для работы с API
- JWT токен администратора PasarGuard панели

## Лицензия

Приватный проект KIZ VPN


